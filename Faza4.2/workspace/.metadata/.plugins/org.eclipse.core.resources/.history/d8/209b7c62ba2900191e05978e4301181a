program MojProgram


const int size = 10, const1=1, const2=2;
const bool b1=true, b2=false;
const char x='a';
const bool bb=true;

int niz[],d;

enum Num { ZERO=0, ONE, TEN = 10, ELEVEN=11 }

int a,b,c;
 
bool bo;
const bool b_const=false;

int xx,aa[];

	class Klasa{
		char a[];
		{
			int sqr(int x){
			
			
				return x*x;
			}
		
		}
	}
{

	void f1(char ch1, int a1, int arg1, bool b1)
		int x;
		bool c;
		Num broj, broj2;
		{
			x=Num.ZERO;
			broj=Num.ZERO;
			broj2=10;
			x = a+1;
			c=false;
 			bo=true;
 
 			ch1='1';
 	 
		}
	int f2(char ch2, int a, int arg)
		int ff;
		int i;
		Num broj;
		{
	
 			f1(ch2,b,broj,bo);
 			niz = new int[20];
			ff=niz[1];
			niz[-niz[niz[broj]]+5*9]=Num.ZERO*2-broj;
		 
			ff=0;
			i=10;
		 
			for(i=0;niz!=niz;i++){
			
				break; 	
			}
 
			return ff;
		}
	int fa(char arr[], char arr2[], bool a[])
	{
		return 2;
	}
	int putp(char ch)
		int x;
		{
			x = 5;
			bo=false;
			x=f2('a',Num.ZERO,3);
			return x;
		}	
	char getp(char ch)
		int x;
		{
			x = -2*a+5*(x/2);
			a = 1;
			b = 1*a;
			c=2;
			return 'x';
		}	
	void main()
	int x, i; char c; int val;
	char l[];
	bool bbb[];
	Num test1, test2;
	Klasa klasa;
	{
		 
		 klasa=new Klasa;
		 klasa.a=new char[10];
		 klasa.a[5]++;
		 print(klasa.a[5]);
		 return;
	}
}