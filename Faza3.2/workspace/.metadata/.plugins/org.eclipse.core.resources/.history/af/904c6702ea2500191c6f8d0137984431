program MojProgram


const int size = 10, const1=1, const2=2;
const bool b1=true, b2=false;
const char x='a';
const bool bb=true;

int niz[],d;

enum Num { ZERO=0, ONE, TEN = 10, ELEVEN=11 }

int a,b,c;
 
bool bo;
const bool b_const=false;
//bool this;
int xx,aa[];
{
	void f1(char ch, int a, int arg)
		int x;
		bool c;
		Num broj, broj2;
		{
			x=Num.ZERO;
			broj=Num.ZERO;
			broj2=10;
			x = a+1;
			c=false;
 			bo=true;
 
 			
 			//a=this;
		}
	int f2(char ch, int a, int arg)
		int ff;
		int i;
		{
	
 			f1(a,b,c);
 			
			//ff=niz[10];
		 
			ff=0;
			i=10;
		 
			for(i=0;niz!=-niz;i++){
			
				break; 	
			}
 
			return ff;
		}
	int putp(char ch)
		int x;
		{
			x = 5;
			bo=false;
			x=f2(1,2,3);
			return x;
		}	
	int getp(char ch)
		int x;
		{
			x =2*a+5*(-2/-a);
			a = 1;
			b = 1*a;
			c=2;
			return x+10;
		}	
	void main()
	int x, i; char c; int val;
	{
	
	 	 c= new char[];
	
		// Initialize val
		val = new int;
		val = new int [size];
		val = new int [size];
		for (i = 0; i<size; i++)
		{
			x=putp(0,i);
			putp(0,i);
		}
		f1(c, x, i);
		
		print(d);
		
		// Read values
		read(x);
		for ( i=1; x>1; )
		{
			if (0 <= x && x < size)
			{
				putp(getp(x)+1);
			} else
			if (-size < x && x < 0)
			{
				putp(getp(-x)+1);
			}
			read(x);
		}
		
		//xx=aa[2];
		 f2();
		 return;
	}
}